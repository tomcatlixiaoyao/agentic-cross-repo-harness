[project]
name = "agentic-cross-repo-harness"
version = "0.3.0"
description = "A provider-neutral control plane for AI-assisted work across Git repositories"
requires-python = ">=3.10"
license = {text = "MIT"}

[tool.ruff]
line-length = 100
target-version = "py310"
